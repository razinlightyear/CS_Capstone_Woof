<div class = 'container' >

    <!-- <div class = 'list-group'>
        <a href = '#' class = 'list-group-item list-group-item-action flex-column align-items-start' >
            <div class = 'd-flex w-100 justify-content-between'>
                <h3 class = 'mb-1'>~Hidden field: event_id</h3>
                <h3 class = 'mb-1'>Event: Lost Dog</h3>
                <h5 class = 'mb-1'>User: username</h5>
            </div>
            <p class = 'mb-1'>Description: This is the description</p>
            <h5 class = 'mb-1'>~View button is going to be here</h5>
        </a>
    </div> -->

    <% @events.each do |event| %>
        <% if event.type == 'LostDog' %>
        <% end %>
    <% end %>


    <div class = 'list-group'>
        <% @events.each do |event| %>
            <% if event.type == 'LostDog' %>
                <%= render 'lost_dogs/create_lostMarker', event: event %>
            <% elsif event.type == 'FoundDog' %>
                <%= render 'found_dogs/create_foundMarker', event: event %>
            <% end %>
        <% end %>
    </div>

    <%= link_to 'Lost Dog Event Modal', lost_dogs_new_path, class: 'btn btn-primary',data: {:remote => true, 'data-toggle' => 'modal', 'data-target'=>'#eventModal'} %>  

    <%= link_to 'Found Dog Event Modal', found_dogs_new_path, class: 'btn btn-primary',data: {:remote => true, 'data-toggle' => 'modal', 'data-target'=>'#eventModal'} %>

</div>

<div class="modal" id="eventModal" tabindex="-1" role="dialog" aria-labelledby="eventLabel" aria-hidden="true">

    <div class="modal-dialog" role="document">
        <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="eventLabel">Dog Event</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body event_content">
        </div>
        </div>
    </div>
    
</div>