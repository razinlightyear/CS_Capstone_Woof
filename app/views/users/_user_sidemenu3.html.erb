    <% if @user.nil? %>
        <% @user  = current_user %>
    <% end %>

    <nav id = 'sidebar' class = 'active'>
        <!-- You have a different class for profile pic. You need not have
        You can use CSS selectors -->
        <div id = 'profile'>
            
            <% unless @user.image.url.nil? %>
                <%= image_tag @user.image.url, class: "rounded-circle", width: 100, height: 100 %>
            <% else %>
                <%= link_to image_tag('woof_logo.png', alt: 'Ur Pic', size: '100x100', class: 'profile-pic rounded-circle'), '#profile-menu', 'aria-expanded': 'false', 'data-toggle': 'collapse' %> 
                
            <% end %>

            <h3><%= "#{@user.first_name} #{@user.last_name}" %></h3>

            <!--<ul class = 'collapse' id = 'profile-menu' style= "list-style: none">
            </ul> -->

        </div>

        <ul class = 'nav nav-tabs flex-column'>
            <!--<li class = 'p-4 nav-item'>-->
            <li>
                <%= link_to 'Around Me Events', event_path(current_user), class: 'nav-link' %>
            </li>
            
            <li>
                <%= link_to  'My Profile', profile_path, class: 'nav-link' %>
            </li>
            
            <li>
                <%= link_to 'My Pets and Groups', groups_pets_path, class: 'nav-link' %>
            </li>
            
            <li>
                <%= link_to 'Logout', home_sign_out_profile_path, class: 'nav-link' %>
            </li>
            
        </ul>
    </nav>